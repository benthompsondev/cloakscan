{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimum surface for the CloakGuard window: one file-export command, user-triggered signed update commands, and app restart after an installed update. No core defaults and no dialog, filesystem, shell, HTTP, clipboard, menu, tray, event, image, window, or devtools permissions.",
  "windows": ["main"],
  "permissions": [
    "allow-export-clean-text",
    "updater:default",
    "process:allow-restart"
  ]
}
